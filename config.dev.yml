server:
  external_url: "http://localhost:8080"
oauth2:
  idp:
    type: "local"
    local:
      users:
        - username: "admin@example.com"
          password: "${MCP_ADMIN_PASSWORD}"
zomboid:
  instances:
    "aaa":
      name: "foo"
      home_dir: "/tmp/foo"
      rcon_host: "localhost"
      users:
        - "admin@example.com"
    "bbb":
      name: "bar"
      home_dir: "/tmp/bar"
      rcon_host: "localhost"
      users:
        - "user@example.com"
